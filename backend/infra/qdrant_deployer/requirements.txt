pulumi>=3.140.0
# The Qdrant Cloud provider is a Terraform-bridged Pulumi provider. Its Python
# SDK is generated locally (not pip-installable from PyPI) via:
#   pulumi package add terraform-provider registry.terraform.io/qdrant/qdrant-cloud
# That command writes the SDK under sdks/ and adds it as a path dependency here.
# See README.md.
sdks/qdrant-cloud
