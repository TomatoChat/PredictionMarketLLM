name: cron-deployer
runtime:
  name: python
  options:
    toolchain: pip
    virtualenv: .venv
description: Deploy each backend/crons/<slug>.yaml as a Cloud Scheduler entry pointing at a backend/apis/<svc>/ route.
